<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- TextViewBase Style -->
    <style name="TextViewBase">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">16dp</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <!-- TextViewBase name Style -->
    <style name="TextViewBase.Name" parent="TextViewBase">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- TextViewBase quantity Style -->
    <style name="TextViewBase.Quantity" parent="TextViewBase">
        <item name="android:textSize">16sp</item>
    </style>

    <!-- TextViewBase price Style -->
    <style name="TextViewBase.Price"  parent="TextViewBase">
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Activity Detail -->
    <style name="ActivityDetailBase">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">16dp</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <!-- TextViewBase Style -->
    <style name="ActivityDetailBase.TextViewBase" parent="ActivityDetailBase">
        <item name="android:textSize">18sp</item>
    </style>

</resources>
